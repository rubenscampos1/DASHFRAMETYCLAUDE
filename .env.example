# Variáveis de Ambiente - FRAMETY

# Database
DATABASE_URL=postgresql://user:password@host:5432/database

# Session (gerar uma string aleatória segura)
SESSION_SECRET=your-super-secret-session-key-here

# Node Environment
NODE_ENV=development

# Port (Render usa a variável PORT automaticamente)
PORT=5000

# Object Storage (AWS S3) - Apenas para Produção (Render.com)
# Deixe vazio em desenvolvimento (usa Replit Object Storage automaticamente)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_BUCKET_NAME=your-bucket-name
AWS_REGION=us-east-1
